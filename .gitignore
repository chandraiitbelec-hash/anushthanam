# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*

# vercel
.vercel

# regenerated by prebuild (scripts/build-search-index.mjs) before every next build
/public/search-index.json

# typescript
*.tsbuildinfo
next-env.d.ts

# working notes / source JSONs for one-off content scripts — never read by app code, stays local
# (decision memos like research/webrtc-vendor-eval-*.md get committed with `git add -f`)
/research

# live-audio spike (self-contained lab bench; never part of a Vercel deploy)
/spike/live-audio/node_modules
/spike/live-audio/public/app.js
